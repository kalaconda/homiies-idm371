import React from 'react';

const Form = () => {
    return(
        <form>
            <input type="text" placeholder="Add todo..." />
            <button type="submit">Add todo</button>
        </form>
    )
}

export default Form;